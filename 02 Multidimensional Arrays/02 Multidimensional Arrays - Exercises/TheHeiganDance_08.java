package com.company.MATRICI_EXERCISES;import java.util.ArrayList;import java.util.List;import java.util.Scanner;public class TheHeiganDance_08 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        double damageHeigan = Double.parseDouble(scanner.nextLine());        String input = scanner.nextLine();        int rows = 15;        int cols = 15;        double[][] matrix = new double[rows][cols];        double playerPoints = 18500;        double heiganPoints = 3000000;        int playerRowPosition = 8;        int playerColPosition = 8;        while (playerPoints >= 0 || heiganPoints >= 0) {            String command = input.split("\\s+")[0];            int startRow = Integer.parseInt(input.split("\\s+")[1]);            int startCol = Integer.parseInt(input.split("\\s+")[2]);            if (command.equals("Cloud")) {                //int[][] spelMatrix = new int[3][3];              /*  for (int row = startRow - 1; row <= startRow + 1; row++) {                    for (int col = startCol + 1; col >= startCol - 1 ; col--) {                        if (row >= 0 && row < matrix.length && col >=0 && col < matrix[row].length){                            if (row == playerRowPosition && col == playerColPosition){                            }                        }                    }                }*/                if (playerRowPosition - 1 > startRow - 1 && playerRowPosition - 1 < matrix.length) {                    playerRowPosition = playerRowPosition - 1;                } else if (playerRowPosition + 1 > startRow && playerRowPosition + 1 < matrix.length){                    playerRowPosition = playerRowPosition + 1;                }                else if (playerColPosition + 1 > startCol                        && playerColPosition + 1 < matrix[playerColPosition + 1].length){                    playerColPosition = playerColPosition + 1;                }            }            input = scanner.nextLine();        }    }}