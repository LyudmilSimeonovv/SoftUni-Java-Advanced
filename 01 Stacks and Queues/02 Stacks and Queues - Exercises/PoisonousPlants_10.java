package com.company.Stekove_Opashki._exersises;import java.util.*;public class PoisonousPlants_10 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int numbersOfPlants = Integer.parseInt(scanner.nextLine());        int[] inputPesticides = Arrays.stream(scanner.nextLine().split("\\s+"))                .mapToInt(Integer::parseInt).toArray();        //ArrayDeque<Integer> pesticides = new ArrayDeque<>();        // Arrays.stream(inputPesticides).forEach(pesticides::offer);        List<Integer> pesticides = new ArrayList<>();        Arrays.stream(inputPesticides).forEach(pesticides::add);        for (int i = pesticides.size() - 1; i >= 0; i--) {            int plantPesticides = pesticides.get(i);            for (int j =pesticides.size() - 2 ; j >= 0; j--) {                    int secondPlantPesticides = pesticides.get(j);                    if (secondPlantPesticides > plantPesticides) {                        pesticides.remove(j);                    } else {                        break;                }            }        }    }}