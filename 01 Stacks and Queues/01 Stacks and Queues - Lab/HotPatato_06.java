package com.company.Stekove_Opashki;import java.lang.reflect.Array;import java.util.*;import java.util.stream.Collectors;public class HotPatato_06 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        //ArrayDeque<String> childrenName = new ArrayDeque<String>(Collections.singleton(scanner.nextLine()));        //ArrayDeque<String> childrenName = new ArrayDeque<>();        //Arrays.stream(scanner.nextLine().split("\\s+")).forEach(childrenName::push);        String[] child = scanner.nextLine().split("\\s+");        ArrayDeque<String> queue = new ArrayDeque<>();        Collections.addAll(queue, child);        int toss = Integer.parseInt(scanner.nextLine());        while(queue.size() > 1){            for (int i = 1; i <toss ; i++) {                queue.offer(queue.poll());            }            System.out.println();            System.out.println("Removed " + queue.poll());        }        System.out.println("Last is " + queue.poll());    }}