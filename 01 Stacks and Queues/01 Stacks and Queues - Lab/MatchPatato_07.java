package com.company.Stekove_Opashki;import java.util.ArrayDeque;import java.util.Collections;import java.util.Scanner;public class MatchPatato_07 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        String[] input = scanner.nextLine().split("\\s+");        ArrayDeque<String> childrenName = new ArrayDeque<>();        Collections.addAll(childrenName, input);        int number = Integer.parseInt(scanner.nextLine());        while (childrenName.size() > 1) {            for (int i = 1; i < number; i++) {                childrenName.offer(childrenName.poll());            }            System.out.printf("Removed %s%n", childrenName.poll());            for (int k = 0; k < childrenName.size(); k++) {                if (checkPrimeNumber(k)) {                   childrenName.offer(childrenName.poll());                    System.out.printf("Prime is %s%n",childrenName.peek());                    //childrenName.offer(childrenName.poll());                    if (childrenName.size() == 1){                        break;                    }                }            }        }        System.out.printf("Last is %s", childrenName.poll());    }    private static boolean checkPrimeNumber(int i) {        boolean isPrimeNumber = true;        for (int j = 2; j <= i; j++) {            if (j % i == 0 && j > 0) {                isPrimeNumber = false;                break;            }        }        return isPrimeNumber;    }}