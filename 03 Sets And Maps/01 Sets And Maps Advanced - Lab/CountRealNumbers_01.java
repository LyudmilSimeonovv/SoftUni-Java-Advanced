package com.company.SET_MAP;import java.text.DecimalFormat;import java.util.Arrays;import java.util.LinkedHashMap;import java.util.Map;import java.util.Scanner;public class CountRealNumbers_01 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        double[] input = Arrays.stream(scanner.nextLine().split("\\s+"))                .mapToDouble(Double::parseDouble).toArray();        Map<Double, Integer> realNumber = new LinkedHashMap<>();        for (double data : input) {            if (!realNumber.containsKey(data)) {                realNumber.put(data, 1);            } else {                realNumber.put(data, realNumber.get(data) + 1);            }        }        DecimalFormat decimalFormat = new DecimalFormat("#.#######");//        realNumber//                .forEach((key, value) -> System.out.printf("%.1f -> %d%n", key, value));        realNumber.forEach((key, value) -> System.out.printf("%s -> %d%n", decimalFormat.format(key), value));    }}